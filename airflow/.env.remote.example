# Remote Server Environment Configuration
# Copy this to .env on your remote Airflow server

# Airflow User ID (use 0 for root, or your user ID)
AIRFLOW_UID=0

# Admin credentials for Airflow web UI
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=change_this_password

# Disable example DAGs (cleaner UI)
AIRFLOW__CORE__LOAD_EXAMPLES=false

# Database configuration (default PostgreSQL in Docker)
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# Path to liquor_app project directory
# Remote: /root/liquor_app
# Local: can leave commented to use default (../)
LIQUOR_APP_DIR=/root/liquor_app

# Database connection for sync scripts
# This is the database where liquor data is stored (separate from Airflow's database)
DB_NAME=liquor_db
DB_USER=jonny
DB_PASSWORD=
DB_HOST=host.docker.internal
DB_PORT=5432

# Optional: Path to catalog-beta project (for TypeScript export)
# Only needed if running the export task
# CATALOG_BETA_HOST_PATH=/root/catalog-beta
